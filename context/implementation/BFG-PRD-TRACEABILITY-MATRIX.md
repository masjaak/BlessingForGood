# BFG PRD Traceability Matrix

Status: `PHASE_07_1_LOCAL_TRACEABILITY_VERIFIED_PRODUCTION_PILOT_BLOCKED`

## Source contract

- Original behavior baseline: archived PRD pack at
  `/Users/masjak/Documents/BLESSINGFORGOOD/BFG WEB/context/product/`.
- Current authority: approved Phase 06.1 Book/Catalog rules, final Phase 06.7
  business policy, database invariants, and the latest explicit Phase 07.1
  decision for one continuous How To Order journey.
- Source gap: `context/SOURCE_OF_TRUTH.md` is absent from this checkout; the
  missing file is not reconstructed from inference.
- No production product data is authorized in this pass without one real
  client-provided product.

| PRD Requirement | Canonical Domain | Admin Route | Admin Action | Backend Query/Mutation | State Transition | Customer Projection | Customer Route | Visual Source | Test | Production Acceptance | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Publisher creation / selection | `publishers` | `/admin/books` | Create publisher, select active publisher for Book Master | `publishers.list`, `publishers.create`, `books.create/update` | No publisher → active publisher → unavailable if inactive | Publisher name only in safe product projection | `/ready-stock`, `/ready-stock/[slug]`, `/catalog` | Admin dashboard 2–3; customer mockups 2–4 | Existing Convex book tests; deterministic full-chain assertion | Create/select one real publisher | `PASS LOCALLY; PILOT BLOCKED` |
| Book Master creation | `books` | `/admin/books` | Submit title metadata | `books.listForAdmin`, `books.create`, `books.getForAdmin` | New → `draft`; update preserves canonical identity | Title, author, description, categories, cover reference when public/private projection allows | `/ready-stock/[slug]` or `/catalog` | Admin dashboard 2–3; customer mockup 4 | Existing create/update tests; deterministic full-chain assertion | Real product title and cover reference | `PASS LOCALLY; PILOT BLOCKED` |
| Variant | `bookVariants` | `/admin/books/[bookId]` | Add/edit format | `bookVariants.listForBook`, `bookVariants.create/update` | No variant → active/inactive variant | Format and variant-specific availability | `/ready-stock/[slug]`, `/catalog` | Customer mockups 2–4 | Existing duplicate/format tests; deterministic full-chain assertion | Real intended format | `PASS LOCALLY; PILOT BLOCKED` |
| ISBN | `bookVariants.isbn` | `/admin/books/[bookId]` | Enter/update ISBN | `bookVariants.create/update` | Missing/duplicate → rejected; valid unique ISBN retained | ISBN is customer-visible where the projection contract requires it | `/ready-stock/[slug]`, `/catalog` | Customer mockups 2–4 | Existing duplicate ISBN test; deterministic projection assertion | Real ISBN | `PASS LOCALLY; PILOT BLOCKED` |
| Price | `bookVariants.priceAmount` | `/admin/books/[bookId]` | Enter/update positive IDR price | `bookVariants.create/update`; order snapshots remain separate | Invalid/non-positive → rejected; valid price on variant | Current customer price or catalog override, never internal cost | `/ready-stock`, `/ready-stock/[slug]`, `/catalog` | Customer mockups 2–4; price rust treatment | Existing positive-money and snapshot tests; deterministic projection assertion | Verify real displayed price; do not edit Production price for QA | `PASS LOCALLY; PILOT BLOCKED` |
| Publication | `books.publicationStatus` | `/admin/books/[bookId]` | Select `draft`, `published`, `special`, or `archived` | `books.update` | `draft` → `published` for public eligibility; `special` for private curation; `archived` hidden | Public and private projections apply server guards | `/ready-stock`, `/ready-stock/[slug]`, `/catalog` | Admin dashboard 2–3; customer mockups 2–4 | Ready Stock isolation + Secret Catalog draft isolation | Publish one real product only if client authorizes | `FIXED; PASS LOCALLY; PILOT BLOCKED` |
| Ready Stock inventory | `readyStockInventory` | `/admin/books/[bookId]` / `/admin/ready-stock` | Set on-hand quantity | `readyStock.setQuantity`; `readyStock.listForAdmin` | No record → unlisted; quantity > reserved → available projection | `available = onHand - reserved`; only positive available variants appear | `/ready-stock`, `/ready-stock/[slug]` | Customer mockup 4; Admin dashboard 2 | Existing positive/zero/reserved tests; customer-safe field assertion | Use one real product only with explicit authorization | `PASS LOCALLY; PILOT BLOCKED` |
| Secret Catalog assignment | `secretCatalogs`, `catalogItems` | `/admin/catalogs` | Create/open catalog, add variant, generate/revoke code | `secretCatalogs.create/createBundle/open/close`, `catalogItems.add`, `catalogAccess.generateCode/revokeCode` | Catalog `draft` → `open` → `closed`; item available/unavailable | `special` or otherwise non-draft active book may appear only in open scoped catalog | `/catalog` | Customer mockups 1–3; Admin dashboard 2–3 | Existing session/authorization tests; draft projection regression | Use a real client catalog only if authorized | `FIXED; PASS LOCALLY; PILOT BLOCKED` |
| Customer Ready Stock visibility | Public book projection | `/admin/books/[bookId]` + inventory | Publish and stock variant | `readyStock.list/getBySlug` | `published + active + stocked + available` → visible; otherwise hidden | Customer-safe book/variant/publisher/price/availability fields only | `/ready-stock`, `/ready-stock/[slug]` | Customer mockup 4; `BFG-CUSTOMER-SURFACE-MATRIX` | Product publishing projection tests + deterministic rendered fixture | Real product pilot required | `PASS LOCALLY; PILOT BLOCKED` |
| Customer Secret Catalog visibility | Scoped catalog projection | `/admin/catalogs` | Open catalog and issue code | `catalogAccess.unlock/getUnlocked` → `getCatalogView` | Valid scoped session + open catalog + active item → visible; invalid/expired/revoked → denied | Title, publisher, variant, ISBN, price/availability; no code digest or internal fields | `/catalog` | Customer mockups 1–3 | Product publishing projection tests + session/revocation tests | Real code/session acceptance required | `PASS LOCALLY; PILOT BLOCKED` |
| Book detail / inline detail behavior | Ready Stock and Secret Catalog customer components | `/admin/books/[bookId]` | Configure fields that feed detail | `readyStock.getBySlug`; `catalogAccess.getUnlocked` | Visible projection → detail; unavailable/hidden → null/empty state | Same safe projection; no admin metadata | `/ready-stock/[slug]`; inline catalog detail/order flow | Customer mockups 3–4 | Product publishing projection tests + deterministic detail render | Real product detail at 390/1440 | `PASS LOCALLY; PILOT BLOCKED` |
| How To Order | Static customer guidance | `/how-to-order` | None | Static `HowToOrderSteps`; CTA links to `/ready-stock` and `/catalog` | Seven semantic moments in one ordered journey | Text-only guidance; no business state mutation | `/how-to-order`, homepage preview `#cara-order` | Customer mockups 2/5/6/7; BFG mobile grammar | Ordered-step/old-card-layout regression + rendered QA | Render at 375/390/430/1440 | `PASS RENDERED LOCALLY` |

## Traceability rule

An arrow is not complete until the backend guard, customer-safe projection,
test, rendered evidence, and (where required) one real client-product pilot
all agree. Existing implementation is evidence, not authority.
