# BFG BUSINESS CONSEQUENCE MATRIX

Reconciled: 2026-08-15
Use this matrix before changing any mutation or workflow. The mutation name is
the canonical backend action; a UI label is never the business contract.

| Mutation | Preconditions | State Transition | Side Effects | Audit | Notification | Customer Projection | Financial Consequence | Invalid Transitions | Tests |
|---|---|---|---|---|---|---|---|---|---|
| `joinRequests.submit` | valid bounded form; no active normalized duplicate | new `submitted` request | optional configured continuation after commit | request submission where defined | admission attention where configured | pending/admission status | none | duplicate active applicant/email/contact; malformed input | Vitest/Convex join tests |
| `joinRequests.startReview` | Admin/Owner; request `submitted` | `submitted → under_review` | reviewer snapshot | review event | Admin queue changes | applicant sees pending state | none | non-submitted request; non-admin | Convex admission tests |
| `joinRequests.approve` / `users.ensureCurrentUser` | approved request; matching Clerk identity/invitation | request approved; app user active | admission link and role mapping | approval/admission event | active-member consequence | private customer surfaces become available | none | Clerk login without approved admission; suspended mapping | Convex auth/admission + Playwright |
| `secretCatalogs.create/open/close` | Admin/Owner; valid dates/name | `draft → open → closed`; archived remains terminal | catalog visibility and access guards change | lifecycle event | access/order attention as applicable | catalog projection opens/closes | none | open closed/archived; edit archived; invalid dates | Convex catalog tests |
| `catalogAccess.generateCode` | Admin/Owner; catalog valid; configured pepper/secret | active code row created | plaintext returned once; digest/lookup stored | code generated | never send plaintext | no customer change until redemption | none | missing secret; invalid catalog; duplicate active policy | Convex security tests |
| `catalogAccess.unlock` | code/session input; active catalog/code; rate limit clear | anonymous access session active | digest/session row; failed attempts on denial | access attempt safe metadata | no credential copy | scoped catalog becomes readable | none | wrong/expired/revoked code; rate limit; out-of-scope catalog | Convex security + Playwright |
| `catalogAccess.grantMember` / `revokeGrant` | Admin/Owner; active customer/catalog | grant active or revoked | access projection changes | access event | safe access notice if configured | customer catalog visibility changes | none | non-customer target; invalid catalog; unauthorized actor | Convex access tests |
| `books.create/update/publish` | Admin/Owner; valid publisher/variant/fields | book/variant draft or publication state | customer projections recalculate; cover reference persists | product event | publication notice where applicable | visible only to allowed projections | price is catalog/order input, not retroactive | invalid ISBN/price/status; unauthorized actor | Vitest/Convex product tests |
| `books.attachCover` | Admin/Owner; storage metadata valid; book exists | cover reference attached/replaced | validated storage URL; old object deleted only after new attachment | media event | none | hard refresh gets durable customer-safe URL | none | invalid type/size/storage; unauthorized actor | Convex storage + Playwright |
| `readyStock.setQuantity` | Admin/Owner; non-negative integer; variant exists | on-hand quantity updated | available projection recalculates | stock event | no fake message | Ready Stock availability updates | none | negative/non-integer; invalid variant | Convex Ready Stock tests |
| `orders.submit` | active customer; valid catalog/session/grant; positive quantity; current variant/price | new `submitted` order and items | server snapshots, idempotency, optional notification | order created | order receipt/workflow event | owned order appears in My Books/Buku Saya | creates commercial snapshot only | no identity; inaccessible catalog; invalid item; duplicate submission | Convex order/policy + Playwright |
| `orders.createReadyStock` | active customer; public eligible variant; available stock | new `submitted` order | atomic reservation row and inventory reserved quantity | reservation/order event | order event | owned Ready Stock order | snapshot/reservation, no payment settlement | insufficient stock; invalid quantity; duplicate/unauthorized | Convex Ready Stock policy tests |
| `orders.createAssisted` | Admin/Owner; active existing customer; valid variant | submitted admin-assisted order | same canonical order/item pipeline | assisted order event | customer order event | customer-owned order appears | same snapshot semantics | non-account manual customer; arbitrary override; inactive target | Convex order tests |
| `batchTracking.updateShipmentStage` | Admin/Owner; batch editable and valid next stage | six-stage forward transition | status history; assignment lock after stage | stage event | tracking notice | owned batch/order tracking updates | none | skip unless explicit allow; backward; archived | Convex state tests |
| `orderFulfillment.updateStage` | Admin/Owner; order and exception guard; no payment-settlement check in this phase | five-stage forward transition | fulfillment history; Ready Stock fulfill at completion | stage event | tracking/fulfillment notice | order detail timeline updates | Ready Stock reservation consumed only at fulfillment | backward/skip/blocked exception | Convex state/policy tests |
| `invoices.create` / `issue` | Admin/Owner; eligible order; valid integer policy | `draft → issued` | immutable items/snapshot, recipient destination | invoice event | issue notification | Tagihan list/detail updates | deposit requirement/outstanding derived | issue invalid/void; missing order; duplicate disallowed path | Convex invoice tests |
| `paymentConfirmations.submit` | active customer owns issued invoice; amount/proof valid | submitted confirmation | private proof storage reference; invoice pending status | payment submitted event | payment review attention | customer sees submitted status | no verified credit yet | own-invoice violation; invalid amount/file/state | Convex payment tests |
| `paymentConfirmations.approve/reject` | Admin/Owner; reviewable confirmation; invoice eligible | under_review → approved/rejected | atomic payment/invoice recalculation; proof retained | payment review event | payment consequence notice | Tagihan status/outstanding updates | approved amount affects settlement only | review twice; wrong invoice; overpayment path | Convex payment/policy tests |
| `depositTopUps.submit/approve/reject` | customer proof or Admin review; valid file/amount | top-up submitted → approved/rejected | approved credit ledger entry | top-up/review event | balance/review notice | Deposit history/balance updates | credit only after approval | duplicate review; invalid proof/amount; unauthorized | Convex deposit tests |
| `invoiceDepositAllocations.allocate/release/reverse` | invoice/customer relation; sufficient available/reserved balance; valid state | allocation active → released/reversed | append-only ledger deltas and allocation record | financial event | invoice/deposit notice where configured | Tagihan and Deposit projections recalculate | no negative balance; release is not cash payout | over-allocation; repeated release; wrong owner/state | Convex financial/policy tests |
| `orderExceptions.requestCancellation/open` | owned active order item; eligibility/state guard | exception `opened` | item-level blocked quantity; no original rewrite | exception event | Admin/customer attention | order detail exception state | none until resolution | fulfilled/cancelled/conflicting/batch/payment invalid | Convex exception tests |
| `orderExceptions.selectResolution/resolve` | Admin review; selected valid resolution; bounded quantity/value | under_review → resolution_selected → resolved | replacement/reference, remove/release, adjustment, or obligation | resolution and financial events | exception/invoice/refund notice | customer order/invoice projection | append-only adjustment; refund obligation if due | unresolved/invalid value; over-refund; double resolve | Convex policy tests |
| `refunds.createPayout/startPayout/recordPayout` | valid obligation; unpaid remaining amount; Admin | payout pending → processing → paid/failed | ledger hold/release/debit; partial paid amount | payout event | refund status notice | customer refund projection | no overpayment; failed retry safe | paid obligation; wrong status; amount exceeds due | Convex refund/policy tests |
| `notifications.markRead` / Inbox read | authenticated recipient; row belongs to recipient | unread → read | `readAt` only | no business audit required | none | unread badge/count changes | none | cross-recipient read; nonexistent row | Convex ownership tests |
| `contentBlocks.upsert/publish` | Admin content permission; bounded approved key | draft → published | public projection updates | content event | no automatic blast | public content changes after publish | none | unsupported key; unauthorized publish | Convex content tests |
| `settings.update` | Owner; bounded store/contact/payment instruction fields | current settings snapshot replaced | customer-safe settings projection | settings event | none | current instructions update | no gateway settlement | non-owner; blank/oversized input | Convex settings/security tests |
| `reports.recordExport` | Admin report permission; bounded period/row count | export event append | CSV generated client-side from canonical report | export audit | none | none | none | unbounded/unauthorized export | Convex reports + Playwright |

## Invariant

Every mutation above must have a server precondition, an explicit state
transition, a consequence projection, and at least one test that fails on an
invalid transition. Adding a visible button without its row here is a
reconciliation failure.
